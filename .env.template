# NVIDIA NGC PyTorch container image (override as needed)
NVIDIA_PYTORCH_IMAGE=nvcr.io/nvidia/pytorch:24.08-py3-igpu

# Default mount: current repo root (where docker-compose.yml lives)
HOST_WORKSPACE=.
